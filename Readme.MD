1. Encapsulate the user with just ID and user
2. Add a few dummy users to the database
3. Encapluste the project backend including:
    -   id
    -   project name
    -   Date from
    -   date to
    -   status 
    -   it has to fetch user from the user table

    connect with frontend